<form class="d-inline" method="post" action="{{ path('app_la_ronde_delete', {'id': la_ronde.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ la_ronde.id) }}">
    <button class="btn">
    <img src="{{asset('images/delete.png')}}" width="35" title="supprimer" alt="supprimer"/>
    </button>
</form>
