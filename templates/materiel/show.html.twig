{% extends 'base.html.twig' %}

{% block title %}Materiel{% endblock %}

{% block body %}
    <h1>Materiel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ materiel.id }}</td>
            </tr>
            <tr>
                <th>Cle</th>
                <td>{{ materiel.cle ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Radio</th>
                <td>{{ materiel.radio ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ materiel.phone ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Ronde</th>
                <td>{{ materiel.ronde ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Lamp</th>
                <td>{{ materiel.lamp ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Contact</th>
                <td>{{ materiel.contact ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Ivvadr</th>
                <td>{{ materiel.ivvadr ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_materiel_index') }}">back to list</a>

    <a href="{{ path('app_materiel_edit', {'id': materiel.id}) }}">edit</a>

    {{ include('materiel/_delete_form.html.twig') }}
{% endblock %}
