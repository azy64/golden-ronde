<form method="post" action="{{ path('app_pointaux_delete', {'id': pointaux.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pointaux.id) }}">
    <div class="text-end">
        <button class="btn btn-danger fs-3 border-0" title="supprimer">X</button>
    </div>
</form>
