{% extends 'base.html.twig' %}

{% block title %}TypeEvenements index{% endblock %}

{% block body %}
    <h1>TypeEvenements index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for type_evenement in type_evenements %}
            <tr>
                <td>{{ type_evenement.id }}</td>
                <td>{{ type_evenement.libelle }}</td>
                <td>
                    <a href="{{ path('app_type_evenements_show', {'id': type_evenement.id}) }}">show</a>
                    <a href="{{ path('app_type_evenements_edit', {'id': type_evenement.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_type_evenements_new') }}">Create new</a>
{% endblock %}
