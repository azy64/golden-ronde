{% extends 'base.html.twig' %}

{% block title %}TypeEvenements index{% endblock %}

{% block body %}
    <h1 class="display-1">Déclencheurs</h1>

    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>#</th>
                <th>Libelle</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for type_evenement in type_evenements %}
            <tr>
                <td>{{ type_evenement.id }}</td>
                <td>{{ type_evenement.libelle }}</td>
                <td>
                    <a class="p-3 text-decoration-none" href="{{ path('app_type_evenements_show', {'id': type_evenement.id}) }}">
                        <img src="{{asset('images/view.png')}}" width="35" title="visualiser" alt="visualiser"/>
                    </a>
                    <a class="p-2 text-decoration-none" href="{{ path('app_type_evenements_edit', {'id': type_evenement.id}) }}">
                        <img src="{{asset('images/editing.png')}}" width="35" title="editer" alt="editer"/>
                    </a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_type_evenements_new') }}">
        <img src="{{asset('images/plus.png')}}" width="35" title="creer" alt="creer"/>
    </a>
{% endblock %}
