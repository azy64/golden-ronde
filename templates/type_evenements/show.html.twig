{% extends 'base.html.twig' %}

{% block title %}TypeEvenements{% endblock %}

{% block body %}
    <h1>TypeEvenements</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_evenement.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ type_evenement.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_evenements_index') }}">back to list</a>

    <a href="{{ path('app_type_evenements_edit', {'id': type_evenement.id}) }}">edit</a>

    {{ include('type_evenements/_delete_form.html.twig') }}
{% endblock %}
